# This file is for my own personal customizations. The goal being to leave 
# .bashrc for the default system settings and to make my customizations in
# .bash_profile which is generally either not present or barely used on most
# fresh user accounts.

# zsh prompt (mac)
PROMPT='%F{cyan}%n%f - %F{red}%*%B%f - %F{blue}%d%f >'

# Alias
alias ll="ls -lah"

# Allow the terminal command line to behave like vim
set -o vi

# Keep a fairly long history and timestamp my entries. 
HISTSIZE=10000
HISTFILESIZE=20000
HISTTIMEFORMAT="%F %T "
